@inject SignInManager<AppUser> signInManager;
@model AboutViewModel
<div class="modal fade" id="modify-@Model.gallery.ID" tabindex="-1" role="dialog" aria-hidden="true">

    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Update Gallery Item</h5>
                <button type="button" class="close" data-dismiss="modal">
                    <span>&times;</span>
                </button>
            </div>
            <form enctype="multipart/form-data" method="post" asp-action="editaboutgalleries" asp-controller="cmsconfiguration" asp-area="management">
                <div class="modal-body">
                    <div class="form-row">
                        <div class="form-group col-md-12">
                            <input asp-for="@Model.gallery.ID" value="@Model.gallery.ID" class="form-control" type="hidden">
                            <input asp-for="@Model.gallery.ImageURL" value="@Model.gallery.ImageURL" class="form-control" type="hidden">
                        </div>
                    </div>

                    <div class="form-row">
                        <div class="form-group col-md-12">
                            <label asp-for="@Model.gallery.Title">Gallery Title</label>
                            <input asp-for="@Model.gallery.Title" class="form-control" placeholder="name">
                            <span asp-validation-for="@Model.gallery.Title" class="text-danger"></span>
                        </div>
                    </div>
                  
               
                    <div class="form-row">
                        <div class="form-group col-md-12">
                            <img src="~/images/gallery/@ViewBag.ImageURL" asp-append-version="true" />
                        </div>
                    </div>
                    <div class="form-row">
                        <div class="form-group col-md-12">
                            <label asp-for="image"></label>
                            <div class="fallback w-100">
                                <input multiple asp-for="image" class="form-control dropify" />
                            </div>
                            <span class="text-danger">make sure you select a  576 x 279 sized image</span>

                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                    <button type="submit" class="btn btn-primary">Save changes</button>
                </div>
            </form>
        </div>
    </div>
</div>
